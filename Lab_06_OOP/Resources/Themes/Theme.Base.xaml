<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="ColorWindowBg">#FFFDF8FC</Color>
    <Color x:Key="ColorPanel">#FFF5EEF8</Color>
    <Color x:Key="ColorAccent">#FFD8A8E8</Color>
    <Color x:Key="ColorAccentDeep">#FFB565D8</Color>
    <Color x:Key="ColorBorder">#FFE8D4EC</Color>
    <Color x:Key="ColorTextMuted">#FF6B5A6E</Color>
    <Color x:Key="ColorStatus">#FFF0E8F4</Color>
    <Color x:Key="ColorSplitter">#FFE8DCEE</Color>

    <SolidColorBrush x:Key="BrushWindowBg" Color="{StaticResource ColorWindowBg}"/>
    <SolidColorBrush x:Key="BrushPanel" Color="{StaticResource ColorPanel}"/>
    <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource ColorAccent}"/>
    <SolidColorBrush x:Key="BrushAccentDeep" Color="{StaticResource ColorAccentDeep}"/>
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}"/>
    <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource ColorTextMuted}"/>
    <SolidColorBrush x:Key="BrushStatus" Color="{StaticResource ColorStatus}"/>
    <SolidColorBrush x:Key="BrushSplitter" Color="{StaticResource ColorSplitter}"/>
    <SolidColorBrush x:Key="BrushForeground" Color="#FF3D2C45"/>

    <SolidColorBrush x:Key="BrushActionBar" Color="#FFE8C4E8"/>
    <SolidColorBrush x:Key="BrushBtnAdd" Color="#FFB8E8B8"/>
    <SolidColorBrush x:Key="BrushBtnDetails" Color="#FFB8E0E8"/>
    <SolidColorBrush x:Key="BrushBtnEdit" Color="#FFFFE08C"/>
    <SolidColorBrush x:Key="BrushBtnDelete" Color="#FFFFC0CB"/>
    <SolidColorBrush x:Key="BrushBtnNeutral" Color="#FFE8E0EC"/>

    <Style x:Key="ConfectioneryButtonChrome" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource BrushForeground}"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Margin" Value="3"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="{DynamicResource BrushAccent}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource BrushBorder}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Opacity" Value="0.82"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Chrome" Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource ConfectioneryButtonChrome}"/>
    <Style x:Key="ActionButton" TargetType="Button" BasedOn="{StaticResource ConfectioneryButtonChrome}">
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Background" Value="{DynamicResource BrushActionBar}"/>
    </Style>

    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushAccentDeep}"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>

    <Style TargetType="Window">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Background" Value="{DynamicResource BrushWindowBg}"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushForeground}"/>
    </Style>
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource BrushForeground}"/>
    </Style>
    <Style TargetType="TextBox">
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="CaretBrush" Value="{DynamicResource BrushAccentDeep}"/>
    </Style>
    <Style TargetType="ComboBox">
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="MinHeight" Value="26"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="Background" Value="White"/>
    </Style>
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource BrushPanel}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>
    <Style TargetType="MenuItem">
        <Setter Property="Padding" Value="8,4"/>
    </Style>
    <Style TargetType="ScrollViewer">
        <Setter Property="Focusable" Value="False"/>
    </Style>
    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{DynamicResource BrushStatus}"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushForeground}"/>
    </Style>
    <Style TargetType="StatusBarItem">
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style TargetType="ListView">
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Padding" Value="2"/>
    </Style>
    <Style TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="2"/>
    </Style>
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource BrushForeground}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="6,3"/>
    </Style>
    <Style TargetType="GridViewColumnHeader">
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource BrushForeground}"/>
    </Style>
    <Style TargetType="Image">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <Style TargetType="Border">
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
    </Style>
    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="{DynamicResource BrushSplitter}"/>
        <Setter Property="Width" Value="6"/>
    </Style>
</ResourceDictionary>
